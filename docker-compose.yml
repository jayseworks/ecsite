version: "3.9"

services:
  web:
    build: .
    volumes:
      - .:/app
      - bundle:/usr/local/bundle
    ports:
      - "3000:3000"
    command: bundle exec rails s -b 0.0.0.0

volumes:
  bundle:
